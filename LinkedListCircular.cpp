/*
    slow pointer - slow= slow->next than slow->next;
    fast pointer - fast= fast->next->next; (Jumping passed nodes)


(If both pointers meet than it can be determined that the LinkedList is a cycle)

*/
#include <iostream>
using namespace std;




int main()
{
    
    
    
    return 0;
}
